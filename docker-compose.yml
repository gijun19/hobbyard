version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: hobbyard-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: hobbyard
      POSTGRES_PASSWORD: hobbyard
      POSTGRES_DB: hobbyard
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U hobbyard']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
